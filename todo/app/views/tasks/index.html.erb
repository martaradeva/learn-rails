<h1><%= I18n.t ('index_page')%></h1>

<%= @greeting %>

<% @tasks.each do |task| %>
	<div>
		<h3><%= link_to task.name, task_path(task)%></h3>
		<%= task.description %>
		<div>
			<%= link_to (I18n.t ('show')), task_path(task) %>
			<%= link_to (I18n.t ('edit')), edit_task_path(task) %>
		</div>
	</div>
<% end -%>


<%= link_to (I18n.t ('new_task_page')), new_task_path %>

<p>

<%= link_to (I18n.t ('home')), root_path %>.